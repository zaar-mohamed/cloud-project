version: "3.8"
services:
  auth-service:
    build: .
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - MONGO_URI=mongodb://mongo:27017/authDB
      - JWT_SECRET=secretKey123
    depends_on:
      - mongo

  mongo:
    image: mongo
    ports:
      - "27017:27017"
